/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { Images } from "./Images";
import { ImagesCountArgs } from "./ImagesCountArgs";
import { ImagesFindManyArgs } from "./ImagesFindManyArgs";
import { ImagesFindUniqueArgs } from "./ImagesFindUniqueArgs";
import { CreateImagesArgs } from "./CreateImagesArgs";
import { UpdateImagesArgs } from "./UpdateImagesArgs";
import { DeleteImagesArgs } from "./DeleteImagesArgs";
import { AnalysisResultsFindManyArgs } from "../../analysisResults/base/AnalysisResultsFindManyArgs";
import { AnalysisResults } from "../../analysisResults/base/AnalysisResults";
import { GoogleGeminiResultsFindManyArgs } from "../../googleGeminiResults/base/GoogleGeminiResultsFindManyArgs";
import { GoogleGeminiResults } from "../../googleGeminiResults/base/GoogleGeminiResults";
import { User } from "../../user/base/User";
import { ImagesService } from "../images.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => Images)
export class ImagesResolverBase {
  constructor(
    protected readonly service: ImagesService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "Images",
    action: "read",
    possession: "any",
  })
  async _imagesItemsMeta(
    @graphql.Args() args: ImagesCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [Images])
  @nestAccessControl.UseRoles({
    resource: "Images",
    action: "read",
    possession: "any",
  })
  async imagesItems(
    @graphql.Args() args: ImagesFindManyArgs
  ): Promise<Images[]> {
    return this.service.imagesItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => Images, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "Images",
    action: "read",
    possession: "own",
  })
  async images(
    @graphql.Args() args: ImagesFindUniqueArgs
  ): Promise<Images | null> {
    const result = await this.service.images(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => Images)
  @nestAccessControl.UseRoles({
    resource: "Images",
    action: "create",
    possession: "any",
  })
  async createImages(@graphql.Args() args: CreateImagesArgs): Promise<Images> {
    return await this.service.createImages({
      ...args,
      data: {
        ...args.data,

        user: args.data.user
          ? {
              connect: args.data.user,
            }
          : undefined,
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => Images)
  @nestAccessControl.UseRoles({
    resource: "Images",
    action: "update",
    possession: "any",
  })
  async updateImages(
    @graphql.Args() args: UpdateImagesArgs
  ): Promise<Images | null> {
    try {
      return await this.service.updateImages({
        ...args,
        data: {
          ...args.data,

          user: args.data.user
            ? {
                connect: args.data.user,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => Images)
  @nestAccessControl.UseRoles({
    resource: "Images",
    action: "delete",
    possession: "any",
  })
  async deleteImages(
    @graphql.Args() args: DeleteImagesArgs
  ): Promise<Images | null> {
    try {
      return await this.service.deleteImages(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [AnalysisResults], {
    name: "analysisResultsItems",
  })
  @nestAccessControl.UseRoles({
    resource: "AnalysisResults",
    action: "read",
    possession: "any",
  })
  async findAnalysisResultsItems(
    @graphql.Parent() parent: Images,
    @graphql.Args() args: AnalysisResultsFindManyArgs
  ): Promise<AnalysisResults[]> {
    const results = await this.service.findAnalysisResultsItems(
      parent.id,
      args
    );

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [GoogleGeminiResults], {
    name: "googleGeminiResultsItems",
  })
  @nestAccessControl.UseRoles({
    resource: "GoogleGeminiResults",
    action: "read",
    possession: "any",
  })
  async findGoogleGeminiResultsItems(
    @graphql.Parent() parent: Images,
    @graphql.Args() args: GoogleGeminiResultsFindManyArgs
  ): Promise<GoogleGeminiResults[]> {
    const results = await this.service.findGoogleGeminiResultsItems(
      parent.id,
      args
    );

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => User, {
    nullable: true,
    name: "user",
  })
  @nestAccessControl.UseRoles({
    resource: "User",
    action: "read",
    possession: "any",
  })
  async getUser(@graphql.Parent() parent: Images): Promise<User | null> {
    const result = await this.service.getUser(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
